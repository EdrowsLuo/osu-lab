package com.edlplan.nso.ruleset.base.beatmap;

import com.edlplan.nso.NsoCore;
import com.edlplan.nso.NsoCoreBased;

/**
 * 内存里保存的Beatmap信息，只保留了最基本的信息
 */
public class BeatmapStorage extends NsoCoreBased {
    public BeatmapStorage(NsoCore core) {
        super(core);
    }
}
